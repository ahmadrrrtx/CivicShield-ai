import { EmptyState } from '@/components/app/empty-state';

export default function ExplorePage() {
  return (
    <EmptyState
      title="Benefit explorer is ready for knowledge integration"
      description="This surface will hold category browsing, jurisdiction filters, and curated public-program discovery once the official source ingestion milestone is connected."
      actionLabel="Return to Ask"
    />
  );
}
